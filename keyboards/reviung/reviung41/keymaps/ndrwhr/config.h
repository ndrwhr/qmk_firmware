#define RGBLIGHT_SLEEP  // allows us to use rgblight_suspend() and rgblight_wakeup() in keymap.c
#define RGBLIGHT_TIMEOUT 60000  // 1 minute.
#define LOCKED_RGB_BRIGHTNESS 48  // Lock brightness at 50% (128 out of 255)
